// Base API URL for backend server
export const API_BASE_URL = 'http://localhost:5000/api'; // Update if deploying

// Supported vibe categories
export const VIBE_CATEGORIES = ['All', 'Romantic', 'Serene', 'Creative'];

// Default map settings (centered on Gwalior)
export const DEFAULT_REGION = {
    latitude: 26.2183,       // Gwalior center
    longitude: 78.1828,
    latitudeDelta: 0.05,
    longitudeDelta: 0.05,
};
